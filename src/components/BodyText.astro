---
interface Props {
	class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<p class:list={["body-text", className]} {...rest}>
	<slot />
</p>

<style>
	.body-text {
		font-family: var(--fontFamilyBody);
	}
</style>
